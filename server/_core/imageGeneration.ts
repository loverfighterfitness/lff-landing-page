/**
 * Image generation — Manus Forge service (removed).
 * This module is unused. Stub retained for type safety.
 */
export async function generateImage(_options: unknown): Promise<{ url: string }> {
  throw new Error("Image generation not available — Manus Forge removed");
}
