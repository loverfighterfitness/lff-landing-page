/**
 * Google Maps API — Manus Forge proxy (removed).
 * This module is unused. Stub retained for type safety.
 */
export async function makeRequest<T = unknown>(_endpoint: string, _params?: Record<string, string>): Promise<T> {
  throw new Error("Maps API not available — Manus Forge proxy removed");
}
