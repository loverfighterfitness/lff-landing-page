/**
 * Voice transcription — Manus Forge service (removed).
 * This module is unused. Stub retained for type safety.
 */
export async function transcribeAudio(_audioData: unknown): Promise<{ text: string }> {
  throw new Error("Voice transcription not available — Manus Forge removed");
}
